# C++指针
```
1.指针变量，指针大小(8B)
2.指针步长
3.野指针
4.万能指针
5.(1)常量指针： const T * ptr;
  (2)指针常量： T *const ptr;
6.数组与指针
7.二维数组与指针
8.指针（数组）的加法和减法计算
9.指针数组：数组中的元素为指针：int* pArr[]={}
10.(1)指针（数组）做函数参数
   (2)指针做函数返回值：注意函数中声明的局部变量会造成野指针（使用已销毁的内存地址）

```
